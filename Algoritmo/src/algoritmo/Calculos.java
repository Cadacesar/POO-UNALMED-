package algoritmo;

public class Calculos {
    public static double calculo_cuadrado (double num) {
        return Math.pow(num, 2);
    }
    public static double calculo_cubo (double num) {
        return Math.pow(num, 3);    
    }
}
